import Vue from 'vue'
import InputField from '@/common/ui/InputField.vue'
import Button from '@/common/ui/Button.vue'

const prefix = 'sn'

Vue.component(`${prefix}-input-field`, InputField)
Vue.component(`${prefix}-button`, Button)
